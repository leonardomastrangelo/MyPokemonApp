//
//  Staging.xcconfig

#include "Base.xcconfig"

API_BASE_URL = http:/$()/my-staging-url.com
API_KEY = my_super_secret_staging_api_key
IS_PRODUCTION = NO
ENV_NAME = staging
PRODUCT_NAME = $(APP_NAME) $(ENV_NAME)
PRODUCT_BUNDLE_IDENTIFIER = $(BASE_BUNDLE_IDENTIFIER).$(ENV_NAME)

